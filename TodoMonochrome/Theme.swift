import SwiftUI

enum Theme {
	// Strict black/gray/white palette (no system accent colors).
	static let bg = Color(white: 0.04)
	static let bgElevated = Color(white: 0.08)
	static let card = Color(white: 0.10)
	static let stroke = Color(white: 0.22)
	static let textPrimary = Color.white
	static let textSecondary = Color(white: 0.72)
	static let textTertiary = Color(white: 0.55)
	static let icon = Color(white: 0.90)
	static let iconMuted = Color(white: 0.55)
	static let shadow = Color.black.opacity(0.55)
}

